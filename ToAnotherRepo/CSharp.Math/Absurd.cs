namespace Metimur.Math {
    public class Absurd {
        // No public constructor, and hence we cannot construct it and return it
        // This ONLY works since we have NullableReference types
        private Absurd() { }
    }
}
